import type { Metadata } from "next";
import VslStudio from "@/features/studio/components/VslStudio";

export const metadata: Metadata = { title: "Editor de VSL", robots: { index: false, follow: false } };

export default VslStudio;
